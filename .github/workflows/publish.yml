name: Publish to PyPI

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build wheels (${{ matrix.platform.name }})
    runs-on: ${{ matrix.platform.runner }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - name: macos-arm64
            runner: macos-14
            cibw-archs: arm64
            cibw-build: cp*-macosx_arm64
          - name: macos-x86_64
            runner: macos-13
            cibw-archs: x86_64
            cibw-build: cp*-macosx_x86_64
          - name: linux-x86_64
            runner: ubuntu-22.04
            cibw-archs: x86_64
            cibw-build: cp*-manylinux_x86_64
          - name: linux-arm64
            runner: ubuntu-22.04-arm
            cibw-archs: aarch64
            cibw-build: cp*-manylinux_aarch64
          - name: windows-x86_64
            runner: windows-latest
            cibw-archs: AMD64
            cibw-build: cp*-win_amd64
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Install cibuildwheel
        run: python -m pip install cibuildwheel build
      - name: Install Rust toolchain (macOS/Windows host)
        if: runner.os != 'Linux'
        uses: dtolnay/rust-toolchain@stable
      - name: Build wheels
        env:
          CIBW_ARCHS: ${{ matrix.platform.cibw-archs }}
          CIBW_BUILD: ${{ matrix.platform.cibw-build }}
          CIBW_BEFORE_ALL_LINUX: >-
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          CIBW_ENVIRONMENT_LINUX: >-
            ZYNK_BUNDLE_CLI=1
            PATH="$HOME/.cargo/bin:$PATH"
          CIBW_ENVIRONMENT_MACOS: >-
            ZYNK_BUNDLE_CLI=1
          CIBW_ENVIRONMENT_WINDOWS: >-
            ZYNK_BUNDLE_CLI=1
        run: python -m cibuildwheel bindings/python --output-dir bindings/python/dist
      - name: Build sdist
        if: matrix.platform.name == 'linux-x86_64'
        run: python -m build bindings/python --sdist --outdir bindings/python/dist
      - name: Store distributions
        uses: actions/upload-artifact@v4
        with:
          name: python-package-distributions-${{ matrix.platform.name }}
          path: bindings/python/dist/

  pypi-publish:
    name: Upload release to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/zynk  
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: python-package-distributions-*
          path: dist/
          merge-multiple: true
      - name: Publish package distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  github-release:
    name: Create GitHub Release
    needs: pypi-publish
    runs-on: ubuntu-latest
    steps:
      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
        run: |
          gh release create "$tag" \
            --repo="$GITHUB_REPOSITORY" \
            --title="zynk ${tag#v}" \
            --generate-notes
