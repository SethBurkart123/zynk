# Kitchen Sink Example

This example demonstrates all the features of Zynk:

- **Users Module**: Basic CRUD operations with Pydantic models
- **Weather Module**: Streaming/channel support with real-time updates
- **Tasks Module**: Complex nested models and enums

## Quick Start

### 1. Start the Backend

```bash
cd backend

# Install Zynk (from the bindings/python project)
pip install -e ../../../bindings/python

# Run the server
python main.py
```

The server listens on `http://127.0.0.1:8100` by default (set via `PORT` env var).

### 1b. Generate the TypeScript Client

Use the standalone `zynk` CLI (shipped via the Python wheel, or `cargo install zynk-cli`):

```bash
# One-shot generation
zynk gen typescript --target python --app main:bridge \
  --out ../frontend/src/generated

# Or run it in watch mode (regenerates whenever main.py / weather.py / etc. change)
zynk dev --app main:bridge --out ../frontend/src/generated
```

### 2. Start the Frontend

In a new terminal:

```bash
cd frontend

# Install dependencies
npm install

# Start Vite dev server
npm run dev
```

Open http://localhost:5173

## Features Demonstrated

### Users Tab
- List users
- Create new users
- Delete users
- Search users
- View user details

### Weather Tab
- Fetch current weather
- 7-day forecast
- **Live streaming** with Server-Sent Events

### Tasks Tab
- Task management with priorities
- Labels/tags
- Statistics dashboard
- Status updates

## Project Structure

```
kitchen-sink/
├── backend/
│   ├── main.py       # Bridge setup & entry point
│   ├── users.py      # User management commands
│   ├── weather.py    # Weather commands with streaming
│   └── tasks.py      # Task management with nested models
└── frontend/
    ├── src/
    │   ├── generated/      # Auto-generated by Zynk
    │   │   ├── api.ts      # Main API client
    │   │   └── _internal.ts
    │   ├── components/
    │   │   ├── UsersDemo.tsx
    │   │   ├── WeatherDemo.tsx
    │   │   └── TasksDemo.tsx
    │   ├── App.tsx
    │   └── main.tsx
    └── package.json
```

## Hot Reload Demo

In two terminals:

```bash
# Terminal 1 — backend with Python hot reload
cd backend && python main.py

# Terminal 2 — TS regeneration on every Python edit
cd backend && zynk dev --app main:bridge --out ../frontend/src/generated
```

Then edit `backend/users.py`:

1. Add a new field to the `User` model
2. Save the file
3. The Python server reloads (its own hot-reload)
4. `zynk dev` respawns the schema dump and regenerates the TS client
5. The frontend updates via Vite HMR!
